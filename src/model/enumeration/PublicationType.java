package model.enumeration;

/**
 * The <code>PublicationType</code> enumeration represents types of publication.
 * 
 * @see model.enumeration.PublicationType#BOOK BOOK
 * @see model.enumeration.PublicationType#JOURNAL JOURNAL
 * @see model.enumeration.PublicationType#CONFERENCE CONFERENCE
 */
public enum PublicationType {
	/** <code>Book</code>, a type of <code>PublicationType</code> enumeration. */
	BOOK,
	
	/** <code>Journal paper</code>, a type of <code>PublicationType</code> enumeration. */
	JOURNAL,
	
	/** <code>Conference paper</code>, a type of <code>PublicationType</code> enumeration. */
	CONFERENCE
}
